---
layout: base
title: Trials of Thought Timeline
description: Landmark trials where ideas were the defendant, from Socrates to Salman Rushdie.
permalink: /trials/timeline/
eleventyExcludeFromCollections: true
---

<div class="container" style="padding-top: var(--space-10); padding-bottom: var(--space-20);">

  <header style="padding-bottom: var(--space-6); border-bottom: 3px solid var(--color-ink); margin-bottom: var(--space-10); text-align: center;">
    <h1 style="font-family: var(--font-masthead); font-size: clamp(1.8rem, 5vw, 3.815rem); margin-bottom: var(--space-4);">Trials of Thought</h1>
    <p style="color: var(--color-ink-muted); font-size: var(--text-md); font-style: italic; max-width: 40ch; margin-inline: auto;">
      When ideas were put on trial, and the verdicts that shaped history.
    </p>
    <div style="margin-top:var(--space-6);display:flex;justify-content:center;">
      <a href="/trials/" class="btn">&larr; All Trials</a>
      <a href="/trials/showcase/" class="btn">Showcase</a>
    </div>
  </header>

  <div class="tl" data-pagefind-ignore>
    <div class="tl__line" aria-hidden="true"></div>

    {% for trial in collections["trials-of-thought"] | sort(false, false, 'data.year') %}

      <div class="tl__event {% if loop.index0 % 2 == 0 %}tl__event--left{% else %}tl__event--right{% endif %}">
        <div class="tl__connector" aria-hidden="true"></div>
        <div class="tl__dot" aria-hidden="true"></div>
        <div class="tl__card">
          <div class="tl__year">{{ trial.data.yearLabel or trial.data.year }}</div>
          <h2 class="tl__title"><a href="{{ trial.url }}">{{ trial.data.title }}</a></h2>
          <p class="tl__desc">{{ trial.data.description }}</p>
        </div>
      </div>

    {% endfor %}
  </div>
</div>

{% include "partials/timeline-styles.njk" %}

<script>
(function () {
  if ('IntersectionObserver' in window) {
    var observer = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) { entry.target.classList.add('is-visible'); observer.unobserve(entry.target); }
      });
    }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });
    document.querySelectorAll('.tl__event').forEach(function (el) { observer.observe(el); });
  } else {
    document.querySelectorAll('.tl__event').forEach(function (el) { el.classList.add('is-visible'); });
  }
})();
</script>
