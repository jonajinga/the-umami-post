---
layout: base
title: Bookshelf Timeline
description: The freethought bookshelf arranged chronologically, from Paine to Pinker.
permalink: /bookshelf/timeline/
eleventyExcludeFromCollections: true
---

<div class="container" style="padding-top: var(--space-10); padding-bottom: var(--space-20);">

  <header style="padding-bottom: var(--space-6); border-bottom: 3px solid var(--color-ink); margin-bottom: var(--space-10); text-align: center;">
    <h1 style="font-family: var(--font-masthead); font-size: clamp(1.8rem, 5vw, 3.815rem); margin-bottom: var(--space-4);">Bookshelf Timeline</h1>
    <p style="color: var(--color-ink-muted); font-size: var(--text-md); font-style: italic; max-width: 40ch; margin-inline: auto;">
      The freethought canon, in the order it was written.
    </p>
    <div style="margin-top:var(--space-6);display:flex;justify-content:center;">
      <a href="/bookshelf/" class="btn">&larr; Bookshelf</a>
    </div>
  </header>

  <div class="tl" data-pagefind-ignore>
    <div class="tl__line" aria-hidden="true"></div>

    {% for book in collections.bookshelf | sort(false, false, 'data.year') %}

      <div class="tl__event {% if loop.index0 % 2 == 0 %}tl__event--left{% else %}tl__event--right{% endif %}">
        <div class="tl__connector" aria-hidden="true"></div>
        <div class="tl__dot" aria-hidden="true"></div>
        <div class="tl__card">
          <div class="tl__year">{{ book.data.year }}</div>
          <h2 class="tl__title"><a href="{{ book.url }}">{{ book.data.title }}</a></h2>
          <p style="font-family:var(--font-ui);font-size:var(--text-xs);color:var(--color-ink-muted);margin:0 0 var(--space-2);">
            {{ book.data.bookAuthor }}
            {% if book.data.category %}<span style="display:inline-block;margin-left:var(--space-2);padding:1px 6px;font-size:10px;border:1px solid var(--color-rule);text-transform:uppercase;letter-spacing:0.06em;">{{ book.data.category }}</span>{% endif %}
            {% if book.data.difficulty %}<span style="display:inline-block;margin-left:var(--space-1);padding:1px 6px;font-size:10px;border:1px solid var(--color-rule);text-transform:uppercase;letter-spacing:0.06em;">{{ book.data.difficulty }}</span>{% endif %}
          </p>
          <p class="tl__desc">{{ book.data.description }}</p>
          {% if book.data.bestFor %}<p style="font-family:var(--font-ui);font-size:var(--text-xs);color:var(--color-ink-faint);margin:var(--space-2) 0 0;font-style:italic;">Best for: {{ book.data.bestFor }}</p>{% endif %}
        </div>
      </div>

    {% endfor %}
  </div>
</div>

{% include "partials/timeline-styles.njk" %}

<script>
(function () {
  if ('IntersectionObserver' in window) {
    var observer = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) { entry.target.classList.add('is-visible'); observer.unobserve(entry.target); }
      });
    }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });
    document.querySelectorAll('.tl__event').forEach(function (el) { observer.observe(el); });
  } else {
    document.querySelectorAll('.tl__event').forEach(function (el) { el.classList.add('is-visible'); });
  }
})();
</script>
