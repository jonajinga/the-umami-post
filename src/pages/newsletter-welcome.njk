---
layout: base
title: You're Subscribed
description: Thanks for subscribing to The Umami Post newsletter.
permalink: /newsletter/welcome/
---

<div class="container nl-page">

  <header class="nl-header">
    <img src="/assets/favicon.svg" alt="" aria-hidden="true" width="48" height="48" class="nl-header__icon">
    <p class="nl-kicker">Newsletter</p>
    <h1 class="nl-title">You&rsquo;re subscribed.</h1>
    <p class="nl-lede">
      Check your inbox for a confirmation email from Buttondown. Click the link inside to activate your subscription &mdash; without it, we can&rsquo;t deliver anything.
    </p>
  </header>

  <section class="nl-block">
    <h2 class="nl-block__title">What to expect</h2>
    <dl class="nl-facts">
      <div class="nl-facts__row">
        <dt>Delivery</dt>
        <dd>New pieces sent when they publish &mdash; no batching, no digest.</dd>
      </div>
      <div class="nl-facts__row">
        <dt>Frequency</dt>
        <dd>As often as we publish. No filler issues to fill a schedule.</dd>
      </div>
      <div class="nl-facts__row">
        <dt>Privacy</dt>
        <dd>Your address is never shared. Every email includes an unsubscribe link.</dd>
      </div>
    </dl>
  </section>

  <div class="nl-footer">
    <p class="nl-footer__note">While you wait for that confirmation email, there&rsquo;s plenty to read.</p>
    <a href="/start-here/" class="btn btn--accent">Start Here &rarr;</a>
  </div>

</div>

<style>
.nl-page {
  max-width: 680px;
  padding-top: var(--space-14);
  padding-bottom: var(--space-20);
}
.nl-header {
  text-align: center;
  margin-bottom: var(--space-8);
  padding-bottom: var(--space-8);
  border-bottom: 3px solid var(--color-ink);
}
.nl-header__icon {
  display: block;
  margin: 0 auto var(--space-4);
  opacity: 0.9;
}
.nl-kicker {
  font-family: var(--font-ui);
  font-size: var(--text-xs);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.18em;
  color: var(--color-accent);
  margin: 0 0 var(--space-3);
}
.nl-title {
  font-family: var(--font-masthead, var(--font-headline));
  font-size: clamp(1.8rem, 5vw, 3.2rem);
  line-height: var(--leading-tight);
  margin: 0 0 var(--space-4);
}
.nl-lede {
  font-family: var(--font-body);
  font-size: var(--text-md);
  color: var(--color-ink-muted);
  line-height: var(--leading-relaxed);
  max-width: 48ch;
  margin: 0 auto;
}
.nl-block {
  padding: var(--space-8) 0;
  margin-bottom: var(--space-8);
  border-bottom: 1px solid var(--color-rule);
}
.nl-block__title {
  font-family: var(--font-ui);
  font-size: var(--text-xs);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.14em;
  color: var(--color-ink);
  padding-bottom: var(--space-2);
  margin: 0 0 var(--space-5);
  border-bottom: 1px solid var(--color-rule);
}
.nl-facts {
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}
.nl-facts__row {
  display: grid;
  grid-template-columns: 120px 1fr;
  gap: var(--space-4);
  font-size: var(--text-sm);
}
.nl-facts dt {
  font-family: var(--font-ui);
  font-size: 0.72rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: var(--color-accent);
  padding-top: 2px;
}
.nl-facts dd {
  color: var(--color-ink-muted);
  line-height: var(--leading-relaxed);
  margin: 0;
}
@media (max-width: 480px) {
  .nl-facts__row { grid-template-columns: 1fr; gap: 4px; }
}
.nl-footer { text-align: center; }
.nl-footer__note {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--color-ink-muted);
  margin: 0 0 var(--space-5);
}
</style>
